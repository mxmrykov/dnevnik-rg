export type Class = {
    pupils: number[],
    capacity: number,
    coach: number,
    classDate: string,
    classTime: string,
    duration: string,
    price: number,
    classType: string,
    isOpen: boolean,
}